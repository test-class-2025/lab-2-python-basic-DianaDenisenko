"""
Назначение: Лабораторная работа №2. Операции с переменными и модуль math.
Автор: Студент
Версия: 1.0
"""
import math


def task_warehouse_correction():
    """
    Задание 1: Исправление категорий и расчет стоимости с НДС.
    Необходимо:
    1. Поменять местами значения категорий (исправить ошибку).
    2. Рассчитать стоимость партии с учетом НДС (20%).
    """
    # --- ВХОДНЫЕ ДАННЫЕ (НЕ МЕНЯТЬ) ---
    category_a = "Vegetables"  # Ошибочно: это овощи, а должны быть фрукты
    category_b = "Fruits"      # Ошибочно: это фрукты, а должны быть овощи
    
    price_per_unit_a = 150  # цена за ящик
    quantity_a = 40         # количество ящиков
    vat_rate = 0.2          # НДС 20%

    # --- НАПИШИТЕ ВАШ КОД НИЖЕ ---

    # 1. Выполните обмен значений переменных category_a и category_b
    # (Используйте синтаксис Python без временной переменной)
    # TODO: ваш код здесь

    # 2. Рассчитайте общую стоимость (total_value) по формуле:
    # (Цена * Кол-во) + (Цена * Кол-во * СтавкаНДС)
    # Не забудьте инициализировать переменную total_value
    total_value = 0.0 # TODO: заменить на формулу

    # --- ВЫВОД РЕЗУЛЬТАТА ---
    print(f"Текущая категория A: {category_a}")
    print(f"Общая стоимость партии с НДС: {total_value}")
    
    # Возвращаем значения для проверки тестами
    return category_a, total_value


def task_zone_calculation():
    """
    Задание 2: Расчет стороны квадратной зоны хранения.
    Необходимо:
    1. Запросить у пользователя ID зоны.
    2. Рассчитать длину стены (корень из площади).
    """
    area_total = 225  # Площадь в кв. метрах (дано)
    zone_id = ""      # Переменная для ID
    side_length = 0.0 # Переменная для длины стены

    # --- НАПИШИТЕ ВАШ КОД НИЖЕ ---

    # 1. Запросите у пользователя идентификатор зоны через input()
    # Сохраните результат в переменную zone_id
    # TODO: ваш код здесь

    # 2. Рассчитайте длину одной стороны (side_length) как корень из area_total
    # Используйте модуль math
    # TODO: ваш код здесь

    # --- ВЫВОД РЕЗУЛЬТАТА ---
    # 3. Выведите отчет (print) по образцу из задания
    # TODO: ваш код здесь (print...)
    
    # Возвращаем значения для проверки тестами
    return zone_id, side_length


if __name__ == "__main__":
    # Этот блок срабатывает, когда вы запускаете файл вручную
    print("--- Задание 1 ---")
    task_warehouse_correction()
    
    print("\n--- Задание 2 ---")
    task_zone_calculation()
